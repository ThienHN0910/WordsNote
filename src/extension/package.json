{
  "name": "extension",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package:edge": "vite build && powershell -Command \"New-Item -ItemType Directory -Path ../../release/microsoft-edge-addon -Force | Out-Null; Compress-Archive -Path dist/* -DestinationPath ../../release/microsoft-edge-addon/WordsNote-Edge-Addon-v1.1.2.zip -Force\""
  },
  "dependencies": {
    "axios": "^1.13.6",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "vite": "^7.3.1"
  }
}
